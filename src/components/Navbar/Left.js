import { styled } from '../../utils'

import SideBase from './SideBase'


const Left = styled(SideBase)({
  justifyContent: 'flex-start',
  whiteSpace: 'nowrap',
})

Left.propTypes = {
}

export default Left
